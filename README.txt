The github url
https://github.com/tia77/HeroesVsMonster

The ~PULL FROM GITHUB.bat is used to refresh the project to the last version on github and redownload the jgrasp project file because the normal refresh would corrupt it.

To use the ~PULL FROM GITHUB.bat script you need to have powershell and git-scm - https://git-scm.com/